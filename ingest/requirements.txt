# JMA ingestion prototypes — dependencies
# Tested with Python 3.10+ in this sandbox (network egress to jma.go.jp / data.jma.go.jp
# was available; see FINDINGS.md).

requests>=2.31
pandas>=2.0
beautifulsoup4>=4.12
lxml>=4.9          # fast HTML parser backend for pandas.read_html / BeautifulSoup
